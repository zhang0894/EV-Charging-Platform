# 充电站与充电桩数据存储与业务逻辑规范

本规范系统性阐明电动汽车充电桩管理平台（EV Charging Platform）在面对 **8,569 座真实充电站、151,143 根充电桩以及高频动态仿真与并发业务** 场景下的分层数据存储架构、运行状态流转模型与核心业务处理逻辑。

---

## 一、 系统数据分层存储架构设计

平台采用 **L1 进程内高性能内存池 + L2 PostgreSQL 持久化关系数据库** 的分层协同架构，严格遵循“**高频运行时数据进内存、低频资产与财务数据入数据库**”的设计原则。

```
+-----------------------------------------------------------------------------------+
|                                 客户端 / 前端应用                                 |
+-----------------------------------------------------------------------------------+
                                         │  HTTP / WebSocket
                                         ▼
+-----------------------------------------------------------------------------------+
|                        服务端接入层 (Boost.Beast / Asio)                           |
+-----------------------------------------------------------------------------------+
       │                                                              │
       │ 高频实时状态 / 空间检索 / 动态过滤                            │ 资产配置 / 财务交易 / 订单归档
       ▼                                                              ▼
+───────────────────────────────────────────────────+   +───────────────────────────+
|               L1 进程内内存状态中心                |   |   L2 PostgreSQL 数据库    |
|                                                   |   |                           |
| 1. StationRTree (8,569 座电站 2D 空间几何索引)     |   | 1. users / user_wallets   |
| 2. ChargingStatePool (151,143 根电桩实时状态)      |   | 2. stations (基础资产信息)|
|    - 运行时状态: IDLE / CHARGING / RESERVED 等    |   | 3. piles (静态物理资产列)  |
|    - 动态遥测: voltage, current, soc, power       |   | 4. charging_orders (订单) |
|    - 累计统计: charge_count, charge_hours         |   | 5. pile_reservations (预约)|
|    - 索引结构: station_pile_ids_ (电站-桩反向索引)|   | 6. wallet_flows (财务流水)|
| 3. StationStatusManager (电站运营/停用状态)        |   | 7. user_avatars (头像)    |
| 4. StationPriceManager (实时电价管理)              |   +───────────────────────────+
+───────────────────────────────────────────────────+
```

### 1.1 L1 进程内内存状态中心（运行时高频领域）
- **适用场景**：高频心跳维护（500ms 刷新）、动态模拟车流状态流转、空间就近检索、多维电站/电桩动态聚合过滤。
- **存储载体**：
  - `StationRTree`：8,569 座电站的空间常量网格与 16 个行政区索引，微秒级计算距离与范围筛选；
  - `ChargingStatePool`：151,143 根充电桩的实时内存结构体 `PileRuntimeState`，包含原子互斥读写锁保护，支撑每秒万级并发查询；
  - `station_pile_ids_`：电站编号到充电桩 ID 列表的反向索引（`std::vector<std::vector<std::string>>`），电站内所有电桩物理有序排列，具备 $O(1)$ 的定位复杂度。

### 1.2 L2 PostgreSQL 持久化数据库（实体资产与强一致性领域）
- **适用场景**：用户账号密码与实名信息、账户钱包余额与支付流水、充电订单历史明细与结算归档、电站/电桩的基础资产档案（桩型号、枪型号、最大额定功率、电压范围等）。
- **事务与一致性保障**：利用 PostgreSQL ACID 事务与行级排他锁（`FOR UPDATE`），保障资金扣减、押金冻结与退还的绝对安全。

## 三、 充电桩与充电站业务逻辑与状态机规范

### 3.1 充电桩状态机定义（Pile Status Machine）
平台支持 7 种核心有效电桩运行状态（已彻底剔除冗余的 MAINTENANCE 状态），内存状态池严格维护以下状态与数字编码对应关系：

| 状态标识 (Status) | 状态编码 (Code) | 状态中文描述 | 语义解释与业务约束 |
| :--- | :---: | :--- | :--- |
| **`IDLE`** | `1` | 空闲可用 | 电桩就绪，无活跃订单且无预约锁定，用户可直接插枪充电或发起预约。 |
| **`PREPARING`** | `2` | 准备中 | 用户已插枪但尚未开始放电（预充阶段）。 |
| **`CHARGING`** | `3` | 充电中 | 正在为车辆输送电能，遥测流持续上报实时电压、电流与 SOC。 |
| **`FINISHING`** | `4` | 充电完成 | 充电结束待拔枪，正在执行计费核算或拔枪等待阶段。 |
| **`FAULT`** | `5` | 设备故障 | 桩端传感器或电力模块告警，不可用，不参与空闲桩统计。 |
| **`OFFLINE`** | `7` | 离线/下线 | 电桩人工下线停用、失去心跳通信连接或所属充电站整体下线。 |
| **`RESERVED`** | `8` | 已预约锁定 | 用户已成功支付 20.00 元押金完成预约锁定，2 分钟内仅预约人可用。 |

### 3.2 充电桩预约业务全生命周期（Reservation Lifecycle）
为了保障高并发场景下绝无超卖且零锁死，预约流程采用 **“内存原子抢占 -> 数据库扣款入账 -> 状态池确认”** 的三阶段防护：

```
[ 用户客户端 ]                  [ 服务端 Controller ]         [ ChargingStatePool (内存) ]    [ PostgreSQL (主库) ]
      │                                 │                                │                           │
      │ 1. POST /charging/reserve       │                                │                           │
      ├────────────────────────────────>│                                │                           │
      │                                 │ 2. 校验用户无生效中订单/预约   │                           │
      │                                 │ 3. 原子抢占锁定 (PENDING)       │                           │
      │                                 ├───────────────────────────────>│                           │
      │                                 │<─── 抢占成功 (占桩排他锁) ──────┤                           │
      │                                 │                                │                           │
      │                                 │ 4. 开启 ACID 事务扣减 20元押金并生成预约单                  │
      │                                 ├──────────────────────────────────────────────────────────>│
      │                                 │<─── 事务提交成功 (生成流水与 RES_ID) ───────────────────────┤
      │                                 │                                │                           │
      │                                 │ 5. 状态升级为正式 RESERVED     │                           │
      │                                 ├───────────────────────────────>│                           │
      │                                 │ 6. WebSocket 广播桩变更为 8     │                           │
      │                                 │                                │                           │
      │<── 200 OK (返回预约成功数据) ───┤                                │                           │
```

1. **原子预占（Pre-lock）**：在访问数据库扣款前，首先调用 `ChargingStatePool::instance().reserve_pile(pile_id, user_id, "PENDING", expire_time)`。若电桩当前非 `IDLE`（例如毫秒级并发冲突），直接返回 `409 Conflict`，**绝不进入数据库事务，绝不扣除用户资金**。
2. **资金冻结与预约单持久化**：在 PostgreSQL 事务中：
   - 检查用户钱包余额是否 $\ge 20.00$ 元（2000 分），扣除 2000 分；
   - 插入 `pile_reservations` 记录，初始状态为 `ACTIVE`，到期时间设定为 $T_{\text{now}} + 120\text{s}$；
   - 记录 `wallet_transaction_flows` 财务流水（类型 5：预约押金支付）。
   - 若事务失败（如余额不足），立即回滚内存状态池，调用 `release_reserved_pile` 将电桩交还回 `IDLE`。
3. **正式锁定与广播**：事务成功后，内存池将该桩绑定真实 `reservation_id` 与到期时间，并通过 WebSocket 广播 `PILE_STATUS_CHANGED`（状态为 `RESERVED`，编码为 8）。
4. **到场履约（Fulfillment）**：用户在 120 秒内插枪启动充电（`POST /api/v1/charging/start`），触发 `fulfill_reservation`：
   - 预约单状态更新为 `FULFILLED`；
   - 20.00 元押金全额原路退还至用户钱包，并记录流水；
   - 电桩状态无缝从 `RESERVED` 切换为 `CHARGING`。
5. **用户主动取消（Cancel）**：
   - 扣除 5.00 元违约取消费用（500 分），退还 15.00 元（1500 分）到用户钱包；
   - 预约单标记为 `CANCELLED`；
   - 内存状态池释放该桩为 `IDLE`，广播 WebSocket 事件。
6. **超时未到场违约处理（Timeout）**：
   - `ChargingSimulator` 动态引擎每秒扫描一次数据库超时的 `ACTIVE` 预约单；
   - 标记单据状态为 `TIMEOUT`，全额没收 20.00 元押金（手续费 20.00 元，退款 0 元）；
   - 内存状态池自动解除预约锁定，恢复电桩为 `IDLE`。
7. **服务端重启防丢机制（Crash Recovery）**：
   - 服务端启动时，在装载静态桩数据后，立即调用 `ChargingStatePool::instance().load_active_reservations_from_db()`；
   - 自动拉取数据库中所有状态为 `ACTIVE` 且尚未超时的预约单，重构内存锁定态，确保服务端更新或重启不影响用户的有效预约权益。

### 3.3 充电站上下线与充电桩状态保真联动规范 (Station Cascading & Fidelity Restoration)
针对电站运维管理（`POST /api/v1/admin/stations/{id}/offline` 与 `online`），系统制定了严格的级联与保真联动规范：
1. **电站下线级联保护（Station Offline Cascading）**：
   - **订单安全结单**：电站下线时，系统遍历该站所有充电桩，若存在充电中订单，服务端自动停止充电、计算费用并在钱包中原子扣款结算，避免账单遗留；若存在预约单则自动释放退还押金。
   - **查询统一呈现为离线**：当充电站处于下线状态时，所有查询接口（`GET /api/v1/piles?station_id={id}` 等）对该站的所有充电桩统一呈现为 `OFFLINE`（状态码 7），且电站空闲可用桩数（`idle_piles`）归零，避免车主误导航或误操作。
2. **电站下线期间状态修改阻断（Offline Protection Lock）**：
   - 当充电站处于下线状态时，服务端**严格拒绝**对该站下属任何充电桩的状态变更操作（`POST /api/v1/admin/piles/{id}/set-status`）及远程重启指令（`POST /api/v1/admin/piles/{id}/restart`），接口均返回 HTTP 404，业务错误码 `20001 StationNotFound`，错误提示 `"充电站已下线，禁止修改其下充电桩状态"`。
3. **电站恢复上线状态保真还原（State Fidelity Restoration）**：
   - 在电站下线时，系统精准记录每个充电桩在下线前的状态快照（`pre_station_offline_status`，仅限三种稳态：`IDLE` 空闲、`FAULT` 故障、`OFFLINE` 人工单独下线；若下线前处于充电或预约中，结单释放后记录为 `IDLE`）。
   - 当管理员执行电站上线操作时，内存状态池**保真恢复**各桩在下线前的真实状态，确保原本故障的桩仍保持 `FAULT`，人工停用的桩仍保持 `OFFLINE`，仅正常空闲的桩恢复为 `IDLE`，杜绝状态漂移或错误唤醒故障设备。

---

## 四、 核心查询接口设计与实现规范

### 4.1 充电站多维综合查询 (`GET /api/v1/stations/inquire`)
统一替代原分散的附近查询、行政区查询与管理端列表接口，提供多维极速检索：
- **支持参数**：
  - `name`：站名模糊检索（中英文不区分大小写）；
  - `district`：行政区限定（支持编码 0~15 或名称如 `"朝阳区"`、`"海淀"`）；
  - `status`：电站运营状态筛选（`1`=营业中，`2`=暂停营业）；
  - `fast_pile`：是否有快充桩能力筛选（`true`=具备快充，`false`=纯慢充）；
  - `latitude` / `longitude`：用户坐标（必须成对提供，自动触发球面距离计算与由近及远升序排序）；
  - `page` / `page_size`：分页（`page_size` 默认 20，强制截断上限 20）。
- **性能实现机制**：
  - 先在 `StationRTree` 与编译期常量 `STATIC_STATIONS` 中根据 `name`、`district`、`status`、`fast_pile` 进行初筛；
  - 初筛出的候选集按距离或 `station_id` 进行排序与分页切片；
  - **按需聚合**：仅针对当前分页窗口内的至多 20 个站点，从 `ChargingStatePool` 中提取实时总桩数、空闲数、快充空闲数、慢充空闲数，耗时 $< 1\text{ms}$。

### 4.2 充电桩全量检索与过滤 (`GET /api/v1/piles`)
- **运行机制**：100% 由 `ChargingStatePool` 内存状态池驱动，零数据库 I/O 损耗。
- **支持参数**：
  - `station_id`：按指定充电站查询；
  - `status`：精确按当前运行时状态筛选（如 `IDLE`、`CHARGING`、`FAULT`、`OFFLINE` 等）；
  - `type`：按快慢充类型筛选（`FAST` / `SLOW`）；
  - `page` / `page_size`：分页（默认 30，上限 30）。
- **严格排序规范**：
  - 当不指定 `station_id` 时，按全网充电桩 ID 字母升序严格递增（如 `P00001_01`, `P00001_02`, ..., `P08565_30`），由于内部采用了按站点递增的反向索引结构，自然保证输出单调递增，无需耗费额外排序算力。
  - **零脏数据保证**：由于过滤条件直接比对内存实时状态，返回的结果行 100% 真实反映桩端最新状态。

---

## 五、 部署与运行运维指导

1. **依赖项配置**：
   - PostgreSQL 数据库（默认连接字符串：`host=127.0.0.1 port=5432 dbname=postgres user=postgres password=Express1. sslmode=disable`）；
   - Redis 服务（可选，若未启动系统会自动降级至进程内线程安全高并发只读缓存）。
2. **启动与非交互模式**：
   - 交互式启动：启动时可输入 `n` 保持现有数据库内容直接启动；
   - 自动化/Docker 模式：配置环境变量 `NO_PROMPT=1`，服务端将跳过标准输入确认，直接启动网络监听。
3. **数据库自检与迁移**：
   - 启动时自动执行 `SchemaMigrator::ensure_schema()`，如遇旧版本遗留的 `piles.status` 列将全自动移除，并补齐可能缺失的核心业务表，无需人工干预。
